import 'package:flutter/cupertino.dart';

class settings_Screen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text("Welcome  settings_Screen", style: TextStyle(fontSize: 20),)
    );
  }
}
